<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-F3w7mX95PdgyTmZZMECAngseQB83DfGTowi0iMjiWaeVhAn4FJkqJByhZMI3AhiU" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-/bQdsTh/da6pkI1MST/rWKFNjaCP5gBSY4sEBT38Q/9RBh9AH40zEOg7Hlq2THRZ" crossorigin="anonymous"></script>
    <script defer src="https://use.fontawesome.com/releases/v5.0.13/js/all.js"></script>
    <title>Home</title>
    <style>
        #app {
            min-height: 100vh;
        }

        .blink_text {
            animation: blinker 1s linear infinite;
        }

        @keyframes blinker {
            50% {
                opacity: 0;
            }
        }
    </style>
</head>
<body>
    <div id="app" class="container-fluid bg-dark text-light">
        {% if app.user %}
            <div class="row pt-5 text-center">
                <div class="col-12">
                    <p class="h2">
                        You are already logged in!
                    </p>
                </div>
                <div class="col-12 mt-3">
                    <div class="row h2 blink_text text-center justify-content-center">
                        <div class="col-3">
                            <i class="fas fa-suitcase"></i> <a href="{{ path("order_index") }}" class="text-decoration-none text-light">Start working</a>
                        </div>
                        <div class="col-3">
                            <i class="fas fa-sign-in-alt"></i> <a href="{{ path("app_logout") }}" class="text-decoration-none text-light">Logout</a>
                        </div>
                    </div>
                </div>

            </div>
        {% else %}
            <div class="row text-center pt-5">
                <div class="col-12">
                    <p class="h2">
                        You have to Login before start your work
                    </p>
                </div>
                <div class="col-12 mt-3">
                    <div class="row h2 blink_text">
                        <div class="col">
                            <i class="fas fa-sign-in-alt"></i> <a href="{{ path("app_login") }}" class="text-decoration-none text-light">Login</a>
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}
    </div>
</body>
</html>